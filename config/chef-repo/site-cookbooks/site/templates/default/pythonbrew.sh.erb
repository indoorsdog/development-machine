if [ -s $HOME/.pythonbrew/etc/bashrc ]; then
  source $HOME/.pythonbrew/etc/bashrc
  if [ ! -s $HOME/.pythonbrew/etc/current ]; then
    pythonbrew use <%= @python %>
    pythonbrew venv use <%= @venv %>
  fi
fi
